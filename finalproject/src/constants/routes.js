const home = "/";
const movies = "/movies";
const signUp = "/signUp";
const signIn = "/signIn";
const notFound = "*";
const series = "/series";
const routes = {
  home,
  movies,
  signUp,
  signIn,
  notFound,
  series,
};

export default routes;
