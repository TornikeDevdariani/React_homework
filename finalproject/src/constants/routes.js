const home = "/";
const movies = "/movies";
const detailPage = `/movies/:movieID`;
const signUp = "/signUp";
const signIn = "/signIn";
const notFound = "*";
const series = "/series";
const routes = {
  home,
  movies,
  signUp,
  signIn,
  notFound,
  series,
  detailPage,
};

export default routes;
